services:
  web:
    build: .
    image: lt-sport-game:latest
    container_name: sportmeet
    ports:
      - "5001:5000"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    environment:
      FLASK_HOST: 0.0.0.0
      FLASK_PORT: 5000
    restart: unless-stopped
